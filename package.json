{
  "name": "stellar-forensics",
  "version": "0.1.0",
  "private": true,
  "description": "Local Stellar secret-key discovery and Horizon verification CLI",
  "type": "module",
  "bin": {
    "stellar-forensics": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/cli.js",
    "test": "node --test",
    "test:functional": "node --test --test-reporter spec",
    "test:headed": "node scripts/headed-smoke.mjs --headed",
    "test:headed:headless": "node scripts/headed-smoke.mjs --headless"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^17.1.0",
    "7zip-bin": "^5.2.0"
  }
}
